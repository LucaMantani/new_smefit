"""LEFT surrogate of smelli's ``fast_likelihood_quarks`` block.

See ``../PLAN.md``.  This package implements the *S1-min* support:
C9, C10 (Re/Im) for ``bsmumu`` and ``bsee`` plus ``CVLL_bsbs``, with smelli's
CKM re-extraction (only gamma moves for this support).
"""
